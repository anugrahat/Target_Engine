# Benchmark Manifest Stubs

These are pre-implementation manifest stubs derived from verified papers.

They are not final production manifests. Their job is to:

- record which datasets are explicitly public
- record which datasets are only partially public
- make unresolved gaps impossible to ignore

Files:

- `ipf_tnik.yaml`
- `hcc_cdk20.yaml`
- `als.yaml`
- `ad_phase_separation.yaml`
