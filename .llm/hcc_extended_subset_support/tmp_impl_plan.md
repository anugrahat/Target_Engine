1. Verify which held-out HCC accession has a clean public case/control definition and usable platform mapping.
2. Extend the real transcriptomics loader to support that accession without inventing identifiers or labels.
3. Add a curated `hcc_adult_extended` subset that keeps the strict core untouched and only broadens HCC evidence with source-backed cohorts.
4. Regenerate registry artifacts, update benchmark docs/configs, and add focused unit coverage.
5. Run validators, live transcriptomics/fused checks, and save a verification report before pushing.
