1. Implement a cached STRING v12 identifier-mapping and interaction-partner loader using the official API.
   - Verify with focused unit tests on patched API responses.
2. Derive a transparent network support score over the top fused candidate slice using weighted connectivity and seed-gene connections.
   - Verify with deterministic unit tests.
3. Expose STRING network evidence through the API and fold it into the fused target ranking as a bounded rerank stage.
   - Verify with route tests.
4. Run the full validation/test suite plus a live smoke test on IPF and HCC.
