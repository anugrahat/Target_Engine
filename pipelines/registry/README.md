# Registry Pipeline

This stage owns generation and validation of dataset-manifest and study-contrast
registry artifacts from curated subset definitions.

Current entry points:

- `ruby scripts/generate_first_wave_registry.rb`
- `ruby scripts/validate_registry_artifacts.rb`
